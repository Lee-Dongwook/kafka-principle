pluginManagement {

}

buildCache {
    local {
        enabled = true
    }
}

rootProject.name = 'kafka-principle'

include 'broker', 'common', 'network', 'connect-core', 'connect-plugin-file', 'image', 'shell'
