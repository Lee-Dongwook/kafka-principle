package io.github.kafkaprinciple.image.node;
import io.github.kafkaprinciple.image.TopicsImage;
/** TODO: render topic metadata. */
public final class TopicsImageNode extends MetadataImagePartNode { public static final String NAME = "topics"; public TopicsImageNode(TopicsImage image) { /* TODO */ } }
