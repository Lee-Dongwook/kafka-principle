package io.github.kafkaprinciple.image.node;

/** Temporary base node for unfinished metadata image sections. */
abstract class MetadataImagePartNode implements MetadataNode {
    // TODO: expose child nodes for each image section.
}
